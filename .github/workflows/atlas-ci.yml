name: ATLAS CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: atlas-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checkout:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Verify repository checkout
        run: test -f README.md && test -f package.json && test -f package-lock.json

  runner-node:
    needs: checkout
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - name: Verify runner toolchain
        run: node --version && npm --version

  install:
    needs: runner-node
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - name: Reproducible install
        run: npm ci

  validate:
    needs: install
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - run: npm ci
      - name: Validate ATLAS contracts and runtime
        run: npm run validate

  cloudflare-contract:
    needs: install
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Verify Cloudflare-native production contract
        run: |
          test -f wrangler.jsonc
          test -f worker/index.js
          test -f worker/secure-entry.js
          test -f migrations/0001_commercial_pilot_core.sql
          test -f migrations/0002_security_events.sql
          grep -q '"main": "worker/secure-entry.js"' wrangler.jsonc
          grep -q '"binding": "DB"' wrangler.jsonc
          grep -q '"crons": \["\*/10 \* \* \* \*"\]' wrangler.jsonc
          grep -q 'ATLAS_BOOTSTRAP_KEY' worker/secure-entry.js
          grep -q 'WRITE_ROLES' worker/secure-entry.js
          grep -q 'atlas_security_events' worker/secure-entry.js
          grep -q 'atlas_memberships' worker/index.js
          grep -q 'authorization' worker/index.js
          grep -q 'actor_user_id' migrations/0001_commercial_pilot_core.sql

  build:
    needs: [validate, cloudflare-contract]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - run: npm ci
      - name: Production build
        run: npm run build:prod
      - name: Require production artifact
        run: test -d dist && test -f dist/index.html
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: atlas-production-build-${{ github.sha }}
          path: dist
          if-no-files-found: error
          retention-days: 7
