{
  "name": "atlas-enterprise-suite-mvp",
  "version": "0.2.1",
  "private": true,
  "description": "ATLAS Core browser MVP with Supabase/PostgreSQL private-beta foundation",
  "scripts": {
    "start": "node server.js",
    "check:js": "node --check app.js && node --check server.js && node --check service-worker.js && node --check cloud-auth.js && node --check atlas-config.js",
    "check:db": "node scripts/validate-database-package.js",
    "validate": "npm run check:js && npm run check:db"
  },
  "engines": {
    "node": ">=18"
  }
}
