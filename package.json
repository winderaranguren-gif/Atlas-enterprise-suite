{
  "name": "atlas-enterprise-suite",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply ATLAS_DB --local",
    "migrate:remote": "wrangler d1 migrations apply ATLAS_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
