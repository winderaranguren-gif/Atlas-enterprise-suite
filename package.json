{
  "name": "atlas-enterprise-suite-mvp",
  "version": "0.2.4",
  "private": true,
  "description": "ATLAS Core functional local and Supabase cloud Private Beta",
  "scripts": {
    "start": "node server.js",
    "check:js": "node --check app.js && node --check server.js && node --check service-worker.js && node --check cloud-auth.js && node --check private-beta.js && node --check atlas-config.js",
    "check:db": "node scripts/validate-database-package.js",
    "validate": "npm run check:js && npm run check:db"
  },
  "engines": {
    "node": ">=18"
  }
}
