const JSON_HEADERS={
  'content-type':'application/json; charset=utf-8',
  'cache-control':'no-store',
  'access-control-allow-origin':'*'
};
const HTML_HEADERS={
  'content-type':'text/html; charset=utf-8',
  'cache-control':'no-store',
  'x-content-type-options':'nosniff',
  'referrer-policy':'same-origin'
};

const CAPABILITIES=[
  {id:'recipe-handoff',name:'Creator Director recipe handoff',state:'ready',engine:'browser-storage'},
  {id:'audio-energy',name:'Audio energy analysis',state:'ready',engine:'web-audio'},
  {id:'motion-energy',name:'Motion energy analysis',state:'ready',engine:'atlas-canvas'},
  {id:'variant-selection',name:'Multi-cut variant selection',state:'ready',engine:'atlas-js'},
  {id:'subject-track',name:'Subject tracking',state:'browser-conditional',engine:'shape-detection-api'},
  {id:'hook-cta',name:'Hook and CTA overlays',state:'ready',engine:'atlas-canvas'},
  {id:'script-captions',name:'Script timed captions',state:'ready',engine:'atlas-js'},
  {id:'audio-cleanup',name:'Voice cleanup',state:'ready',engine:'web-audio'},
  {id:'batch-render',name:'Batch render variants',state:'ready',engine:'media-recorder'},
  {id:'external-provider',name:'External creative provider',state:'not-required',engine:'none'}
];

function manifest(){
  return {
    service:'atlas-studio-autopilot',
    version:1,
    ownership:'ATLAS first-party',
    externalProviders:[],
    externalProviderRequired:false,
    mediaLeavesDeviceByDefault:false,
    capabilities:CAPABILITIES
  };
}
function json(data,status=200){return new Response(JSON.stringify(data,null,2),{status,headers:JSON_HEADERS});}

function page(){
  const cfg=JSON.stringify(manifest()).replace(/</g,'\\u003c');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>ATLAS Studio Autopilot</title><style>
:root{--bg:#020711;--panel:#071725;--panel2:#0a2034;--line:#1d4768;--text:#f4f9ff;--muted:#8fa8bc;--cyan:#54d9ff;--green:#52dda2;--amber:#ffd16e;--red:#ff7580}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 82% -8%,#134b7f 0,transparent 34%),var(--bg);color:var(--text);font:14px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.app{max-width:1500px;margin:auto;padding:18px}.hero,.card{border:1px solid var(--line);background:linear-gradient(145deg,#0a1f34,#05101b);border-radius:16px}.hero{padding:22px;display:flex;gap:18px;align-items:flex-start}.hero h1{margin:4px 0 7px;font-size:31px}.hero p{margin:0;color:var(--muted);max-width:900px;line-height:1.55}.pill{margin-left:auto;border:1px solid #2d6b52;background:#08271e;color:#aaefcc;border-radius:999px;padding:7px 10px;font-size:10px;white-space:nowrap}.grid{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(340px,.75fr);gap:12px;margin-top:12px}.card{padding:14px;min-width:0}.card h2{font-size:14px;margin:0 0 10px}.stage{min-height:520px;display:grid;place-items:center;background:#000;border:1px solid #244e70;border-radius:12px;overflow:hidden;position:relative}.stage video{display:none}.stage canvas{max-width:100%;max-height:70vh;background:#000}.empty{color:#6f8da5;text-align:center}.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.btn,.field{border:1px solid #2a6188;background:#0a2a45;color:#fff;border-radius:9px;padding:9px 11px}.btn{cursor:pointer}.btn.primary{background:linear-gradient(135deg,#147ce1,#16a9d5);border-color:#44c8f0}.btn:disabled{opacity:.45;cursor:not-allowed}.field{width:100%;margin:5px 0 9px;background:#06111d}.row{display:grid;grid-template-columns:1fr 1fr;gap:8px}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:9px}.metric,.status{border:1px solid #1d425e;background:#071421;border-radius:9px;padding:9px}.metric span{display:block;color:#7893a9;font-size:9px}.metric b{display:block;margin-top:4px;font-size:11px}.status{color:#9eb6c9;font-size:10px;line-height:1.45}.good{border-color:#2c684f;color:#a8eccb;background:#08271e}.warn{border-color:#6b5a2c;color:#ffe1a0;background:#2a220d}.variant{display:grid;grid-template-columns:62px 1fr 90px;gap:8px;padding:9px 0;border-bottom:1px solid #173650}.variant small{color:#7f99af}.score{font-weight:800;color:#a8eccb;text-align:right}.download{display:block;border:1px solid #2b694f;background:#08281e;border-radius:9px;padding:9px;margin-top:7px}.download a{color:#a7edcc}.progress{height:7px;border-radius:99px;background:#06101b;border:1px solid #19384e;overflow:hidden;margin:9px 0}.progress div{height:100%;width:0;background:linear-gradient(90deg,#277ff0,#58ddff)}.log{height:145px;overflow:auto;background:#020811;border:1px solid #173650;border-radius:8px;padding:8px;font:10px ui-monospace,monospace;color:#9fc8e3}.recipe{max-height:160px;overflow:auto;white-space:pre-wrap;word-break:break-word;color:#a8c5d9;font:10px ui-monospace,monospace}.bar{height:6px;background:#06101b;border:1px solid #183850;border-radius:99px;overflow:hidden;margin-top:5px}.bar i{display:block;height:100%;background:linear-gradient(90deg,#167edb,#56d9ff)}@media(max-width:980px){.grid{grid-template-columns:1fr}.stage{min-height:390px}}@media(max-width:620px){.app{padding:10px}.hero{display:block}.pill{display:inline-block;margin:10px 0 0}.row,.metrics{grid-template-columns:1fr 1fr}.variant{grid-template-columns:52px 1fr 72px}}
</style></head><body><main class="app"><section class="hero"><div><small style="color:var(--cyan);letter-spacing:.13em">ATLAS FIRST-PARTY VIDEO AUTOPILOT</small><h1>Studio Autopilot</h1><p>Analyze a local video, combine motion and audio energy, find the strongest non-overlapping moments, track a subject when the browser supports native face detection, apply Creator Director hook and CTA overlays, and batch render multiple variants without sending media to an external creative provider.</p></div><span class="pill">0 external providers</span></section>
<div class="grid"><section class="card"><h2>Source and preview</h2><div class="stage"><video id="video" playsinline preload="metadata"></video><canvas id="canvas" width="540" height="960"></canvas><div id="empty" class="empty">Choose a local video. Media remains on this device.</div></div><div class="toolbar"><label class="btn primary" for="file">Choose video</label><input id="file" type="file" accept="video/*" hidden><button class="btn" id="play" disabled>Play</button><button class="btn" id="nativeStudio">Open Native Studio</button><button class="btn" id="director">Creator Director</button></div><div class="metrics"><div class="metric"><span>DURATION</span><b id="duration">-</b></div><div class="metric"><span>SOURCE</span><b id="source">-</b></div><div class="metric"><span>RECIPE</span><b id="recipeState">none</b></div><div class="metric"><span>SUBJECT TRACK</span><b id="trackState">checking</b></div></div></section>
<section class="card"><h2>Autopilot controls</h2><div id="recipeNotice" class="status">No Creator Director recipe loaded yet. Autopilot can still analyze the source.</div><div class="row"><label>Cut seconds<input class="field" id="cutSeconds" type="number" min="5" max="60" value="15"></label><label>Variants<input class="field" id="variantCount" type="number" min="1" max="5" value="3"></label></div><label>Output format<select class="field" id="format"><option value="9:16">9:16 Reel / Short</option><option value="16:9">16:9 Landscape</option><option value="1:1">1:1 Square</option></select></label><div class="row"><label>Motion weight<input class="field" id="motionWeight" type="range" min="0" max="100" value="55"></label><label>Audio weight<input class="field" id="audioWeight" type="range" min="0" max="100" value="45"></label></div><label>Hook overlay<input class="field" id="hook" maxlength="220" placeholder="Opening hook"></label><label>CTA overlay<input class="field" id="cta" maxlength="220" placeholder="Closing call to action"></label><textarea class="field" id="script" rows="5" placeholder="Script for timed captions"></textarea><label><input id="captions" type="checkbox" checked> Burn script captions</label><label><input id="cleanup" type="checkbox" checked> Apply voice cleanup during render</label><label><input id="tracking" type="checkbox" checked> Use native subject tracking when available</label><button class="btn primary" id="analyze" disabled>Analyze and build variants</button><div id="analysisStatus" class="status" style="margin-top:9px">Load media first.</div></section></div>
<div class="grid"><section class="card"><h2>Selected variants</h2><div id="variants"><div class="status">Run analysis to build cut candidates.</div></div><button class="btn primary" id="renderAll" disabled style="margin-top:10px">Render all variants</button><button class="btn" id="cancel" disabled>Cancel</button><div class="progress"><div id="progress"></div></div><div id="downloads"></div></section><section class="card"><h2>Creator recipe</h2><div class="recipe" id="recipeRaw">No recipe in browser storage.</div><h2 style="margin-top:14px">Production log</h2><div class="log" id="log"></div></section></div></main>
<script>
const CFG=${cfg};const e=id=>document.getElementById(id);const video=e('video'),canvas=e('canvas'),ctx=canvas.getContext('2d',{alpha:false});const sample=document.createElement('canvas');sample.width=96;sample.height=54;const sctx=sample.getContext('2d',{willReadFrequently:true});let file=null,fileUrl=null,recipe=null,variants=[],motionSeries=[],audioSeries=[],trackSeries=[],cues=[],rendering=false,cancelled=false,audioGraph=null,lastRenderUrl=[];
function log(msg){const d=document.createElement('div');d.textContent=new Date().toLocaleTimeString()+'  '+msg;e('log').prepend(d)}function fmt(s){if(!Number.isFinite(s))return '-';const m=Math.floor(s/60),x=Math.floor(s%60);return m+':'+String(x).padStart(2,'0')}function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function esc(s){return String(s||'').replace(/[&<>"']/g,function(m){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]})}
function loadRecipe(){try{recipe=JSON.parse(localStorage.getItem('atlas.creator.recipe')||'null')}catch{recipe=null}if(!recipe){e('recipeState').textContent='none';return}e('recipeState').textContent='loaded';e('recipeNotice').className='status good';e('recipeNotice').textContent='Creator Director recipe loaded. Autopilot will use its format, duration, hook, CTA and script.';e('recipeRaw').textContent=JSON.stringify(recipe,null,2);if(recipe.production){e('cutSeconds').value=clamp(Number(recipe.production.duration)||15,5,60);if(['9:16','16:9','1:1'].includes(recipe.production.format))e('format').value=recipe.production.format}if(recipe.creative){e('hook').value=recipe.creative.hook||'';e('cta').value=recipe.creative.cta||'';e('script').value=recipe.creative.script||''}log('Creator Director recipe imported')}
function dims(){const f=e('format').value,r=f==='9:16'?9/16:f==='16:9'?16/9:1,base=720;return r<1?{w:base,h:Math.round(base/r)}:{w:Math.round(base*r),h:base}}
function splitSentences(text){return String(text||'').trim().split(/(?<=[.!?])\\s+|\\n+/).map(function(x){return x.trim()}).filter(Boolean).slice(0,80)}function buildCues(start,end){const lines=splitSentences(e('script').value);if(!lines.length)return[];const total=end-start,each=total/lines.length;return lines.map(function(text,i){return {start:start+i*each,end:start+(i+1)*each,text:text}})}function activeCue(t){return cues.find(function(c){return t>=c.start&&t<c.end})}
function seek(t){return new Promise(function(resolve){const done=function(){video.removeEventListener('seeked',done);resolve()};video.addEventListener('seeked',done,{once:true});video.currentTime=clamp(t,0,Math.max(0,video.duration-.03))})}
function luminanceFrame(){sctx.drawImage(video,0,0,sample.width,sample.height);const p=sctx.getImageData(0,0,sample.width,sample.height).data,out=new Uint8Array(sample.width*sample.height);for(let i=0,j=0;i<p.length;i+=4,j++)out[j]=Math.round(.2126*p[i]+.7152*p[i+1]+.0722*p[i+2]);return out}function frameDelta(a,b){if(!a||!b)return 0;let sum=0;for(let i=0;i<a.length;i+=4)sum+=Math.abs(a[i]-b[i]);return sum/(a.length/4)/255}
async function analyzeMotion(step){const out=[];let prev=null;for(let t=0;t<video.duration;t+=step){if(cancelled)throw new Error('Analysis cancelled');await seek(t);const frame=luminanceFrame(),score=frameDelta(frame,prev);out.push({t:t,v:score});prev=frame;e('progress').style.width=Math.min(45,(t/video.duration)*45)+'%'}return out}
async function analyzeAudio(step){const ab=await file.arrayBuffer(),ac=new (window.AudioContext||window.webkitAudioContext)(),buffer=await ac.decodeAudioData(ab.slice(0)),data=buffer.getChannelData(0),rate=buffer.sampleRate,windowSamples=Math.max(1,Math.round(step*rate)),out=[];for(let start=0;start<data.length;start+=windowSamples){let sum=0,count=Math.min(windowSamples,data.length-start);for(let i=0;i<count;i+=8){const x=data[start+i];sum+=x*x}const sampled=Math.ceil(count/8),rms=Math.sqrt(sum/Math.max(1,sampled));out.push({t:start/rate,v:rms})}await ac.close();return out}
function normalize(series){const vals=series.map(function(x){return x.v}),min=Math.min.apply(null,vals),max=Math.max.apply(null,vals),span=max-min||1;return series.map(function(x){return {t:x.t,v:(x.v-min)/span}})}function valueAt(series,t){if(!series.length)return 0;let idx=Math.min(series.length-1,Math.max(0,Math.round(t/(series[1]?series[1].t-series[0].t:.5))));return series[idx]?series[idx].v:0}
function scoreWindow(start,len,step,mw,aw){let m=0,a=0,n=0;for(let t=start;t<start+len;t+=step){m+=valueAt(motionSeries,t);a+=valueAt(audioSeries,t);n++}m/=Math.max(1,n);a/=Math.max(1,n);return {motion:m,audio:a,total:m*mw+a*aw}}
function selectVariants(len,count,step){const mw=Number(e('motionWeight').value),aw=Number(e('audioWeight').value),sum=mw+aw||1,candidates=[];for(let start=0;start+len<=video.duration;start+=step){const s=scoreWindow(start,len,step,mw/sum,aw/sum);candidates.push({start:start,end:start+len,motion:s.motion,audio:s.audio,total:s.total})}candidates.sort(function(a,b){return b.total-a.total});const picked=[];for(const c of candidates){if(picked.every(function(p){return Math.min(p.end,c.end)-Math.max(p.start,c.start)<len*.25})){picked.push(c);if(picked.length>=count)break}}return picked.sort(function(a,b){return a.start-b.start}).map(function(v,i){return Object.assign({index:i+1},v)})}
async function trackSubjectForVariants(step){trackSeries=[];if(!e('tracking').checked||!('FaceDetector' in window)){e('trackState').textContent='center fallback';return}try{const detector=new FaceDetector({fastMode:true,maxDetectedFaces:1});for(const v of variants){for(let t=v.start;t<=v.end;t+=step){if(cancelled)throw new Error('Tracking cancelled');await seek(t);const faces=await detector.detect(video);if(faces[0]){const b=faces[0].boundingBox;trackSeries.push({t:t,x:(b.x+b.width/2)/(video.videoWidth||1)})}}}e('trackState').textContent=trackSeries.length?'native active':'center fallback'}catch(err){e('trackState').textContent='center fallback';log('Native face tracking unavailable: '+err.message)}}
function centerAt(t){if(!trackSeries.length)return .5;let best=trackSeries[0];for(const x of trackSeries){if(Math.abs(x.t-t)<Math.abs(best.t-t))best=x}return clamp(best.x,.2,.8)}
function drawTextBox(text,y,w,h,accent){if(!text)return;const size=Math.max(24,Math.round(w*.045));ctx.save();ctx.font='800 '+size+'px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';const lines=[];const words=String(text).split(/\\s+/);let line='';for(const word of words){const test=(line+' '+word).trim();if(ctx.measureText(test).width>w*.78&&line){lines.push(line);line=word}else line=test}if(line)lines.push(line);const shown=lines.slice(0,3),boxH=shown.length*size*1.22+size*.8;ctx.fillStyle='rgba(2,7,17,.76)';ctx.fillRect(w*.08,y-boxH/2,w*.84,boxH);ctx.fillStyle=accent||'#fff';shown.forEach(function(l,i){ctx.fillText(l,w/2,y+(i-(shown.length-1)/2)*size*1.22)});ctx.restore()}
function drawFrame(t,w,h,start,end){const sw=video.videoWidth||1,sh=video.videoHeight||1,src=sw/sh,dst=w/h,cx=centerAt(t)*sw;ctx.fillStyle='#000';ctx.fillRect(0,0,w,h);let sx=0,sy=0,sww=sw,shh=sh;if(src>dst){sww=sh*dst;sx=clamp(cx-sww/2,0,sw-sww)}else{shh=sw/dst;sy=(sh-shh)/2}ctx.filter='brightness(103%) contrast(108%) saturate(108%)';ctx.drawImage(video,sx,sy,sww,shh,0,0,w,h);ctx.filter='none';const hook=e('hook').value.trim(),cta=e('cta').value.trim();if(hook&&t-start<=2.8)drawTextBox(hook,h*.18,w,h,'#ffffff');if(cta&&end-t<=3.2)drawTextBox(cta,h*.82,w,h,'#9ee8ff');if(e('captions').checked){const cue=activeCue(t);if(cue)drawTextBox(cue.text,h*.7,w,h,'#ffffff')}}
function previewLoop(){if(file&&!video.paused&&!video.ended){const d=dims();if(canvas.width!==d.w||canvas.height!==d.h){canvas.width=d.w;canvas.height=d.h}drawFrame(video.currentTime,d.w,d.h,0,video.duration);requestAnimationFrame(previewLoop)}}
async function ensureAudioGraph(){if(audioGraph)return audioGraph;const AC=window.AudioContext||window.webkitAudioContext,ac=new AC(),source=ac.createMediaElementSource(video),high=ac.createBiquadFilter(),comp=ac.createDynamicsCompressor(),dest=ac.createMediaStreamDestination();high.type='highpass';high.frequency.value=80;comp.threshold.value=-24;comp.knee.value=18;comp.ratio.value=3;comp.attack.value=.01;comp.release.value=.22;source.connect(high).connect(comp);comp.connect(dest);comp.connect(ac.destination);audioGraph={ac:ac,dest:dest};return audioGraph}
function recorderMime(){const options=['video/webm;codecs=vp9,opus','video/webm;codecs=vp8,opus','video/webm'];return options.find(function(x){return MediaRecorder.isTypeSupported(x)})||''}
async function renderVariant(v){const d=dims();canvas.width=d.w;canvas.height=d.h;cues=buildCues(v.start,v.end);const fps=30,stream=canvas.captureStream(fps),graph=await ensureAudioGraph();if(e('cleanup').checked){graph.dest.stream.getAudioTracks().forEach(function(t){stream.addTrack(t)})}else if(video.captureStream){const raw=video.captureStream();raw.getAudioTracks().forEach(function(t){stream.addTrack(t)})}const mime=recorderMime(),chunks=[],rec=new MediaRecorder(stream,mime?{mimeType:mime}:undefined);rec.ondataavailable=function(ev){if(ev.data&&ev.data.size)chunks.push(ev.data)};const stopped=new Promise(function(resolve){rec.onstop=resolve});await seek(v.start);await graph.ac.resume();rec.start(250);await video.play();const loop=function(){if(cancelled||video.currentTime>=v.end){video.pause();if(rec.state!=='inactive')rec.stop();return}drawFrame(video.currentTime,d.w,d.h,v.start,v.end);e('progress').style.width=(55+((video.currentTime-v.start)/(v.end-v.start))*40)+'%';requestAnimationFrame(loop)};loop();await stopped;stream.getTracks().forEach(function(t){t.stop()});return new Blob(chunks,{type:mime||'video/webm'})}
function renderVariantList(){e('variants').innerHTML=variants.length?variants.map(function(v){return '<div class="variant"><b>#'+v.index+'</b><div><b>'+fmt(v.start)+' to '+fmt(v.end)+'</b><small><br>Motion '+Math.round(v.motion*100)+'% | Audio '+Math.round(v.audio*100)+'%</small><div class="bar"><i style="width:'+Math.round(v.total*100)+'%"></i></div></div><div class="score">'+Math.round(v.total*100)+'%</div></div>'}).join(''):'<div class="status">No variants found.</div>';e('renderAll').disabled=!variants.length}
e('file').onchange=function(ev){file=ev.target.files[0];if(!file)return;if(fileUrl)URL.revokeObjectURL(fileUrl);fileUrl=URL.createObjectURL(file);video.src=fileUrl;video.load();e('empty').style.display='none';video.onloadedmetadata=function(){e('duration').textContent=fmt(video.duration);e('source').textContent=(video.videoWidth||0)+'x'+(video.videoHeight||0);e('analyze').disabled=false;e('play').disabled=false;e('analysisStatus').textContent='Ready for local analysis.';const d=dims();canvas.width=d.w;canvas.height=d.h;seek(0).then(function(){drawFrame(0,d.w,d.h,0,video.duration)});log('Loaded '+file.name+' locally')}};
e('play').onclick=function(){if(video.paused){video.play();previewLoop()}else video.pause()};e('nativeStudio').onclick=function(){location.href='/studio/production'};e('director').onclick=function(){location.href='/studio/director'};
e('analyze').onclick=async function(){if(!file)return;cancelled=false;e('analyze').disabled=true;e('renderAll').disabled=true;e('analysisStatus').className='status';e('analysisStatus').textContent='Analyzing motion and audio energy locally...';e('progress').style.width='2%';try{const step=.5;motionSeries=normalize(await analyzeMotion(step));e('progress').style.width='48%';audioSeries=normalize(await analyzeAudio(step));e('progress').style.width='55%';const len=clamp(Number(e('cutSeconds').value)||15,5,Math.min(60,video.duration)),count=clamp(Number(e('variantCount').value)||3,1,5);variants=selectVariants(len,count,step);await trackSubjectForVariants(1);renderVariantList();e('analysisStatus').className='status good';e('analysisStatus').textContent='Built '+variants.length+' non-overlapping variants from combined motion and audio energy.';e('progress').style.width='100%';log('Autopilot analysis complete: '+variants.length+' variants')}catch(err){e('analysisStatus').className='status warn';e('analysisStatus').textContent=err.message;log('Analysis failed: '+err.message)}finally{e('analyze').disabled=false;setTimeout(function(){if(!rendering)e('progress').style.width='0%'},900)}};
e('renderAll').onclick=async function(){if(!variants.length)return;rendering=true;cancelled=false;e('renderAll').disabled=true;e('cancel').disabled=false;e('downloads').innerHTML='';lastRenderUrl.forEach(function(u){URL.revokeObjectURL(u)});lastRenderUrl=[];try{for(let i=0;i<variants.length;i++){if(cancelled)break;const v=variants[i];e('progress').style.width='55%';log('Rendering variant '+v.index);const blob=await renderVariant(v),u=URL.createObjectURL(blob);lastRenderUrl.push(u);const box=document.createElement('div');box.className='download';const a=document.createElement('a');a.href=u;a.download='atlas-autopilot-variant-'+v.index+'.webm';a.textContent='Download variant '+v.index+' ('+Math.round(blob.size/1024/1024*10)/10+' MB)';box.appendChild(a);e('downloads').appendChild(box)}if(!cancelled){e('progress').style.width='100%';log('Batch render complete')}else log('Batch render cancelled')}catch(err){log('Render failed: '+err.message);e('downloads').innerHTML='<div class="status warn">'+esc(err.message)+'</div>'}finally{rendering=false;e('cancel').disabled=true;e('renderAll').disabled=false;setTimeout(function(){e('progress').style.width='0%'},1000)}};
e('cancel').onclick=function(){cancelled=true;video.pause()};window.addEventListener('beforeunload',function(){if(fileUrl)URL.revokeObjectURL(fileUrl);lastRenderUrl.forEach(function(u){URL.revokeObjectURL(u)})});loadRecipe();e('trackState').textContent=('FaceDetector' in window)?'native available':'center fallback';log('ATLAS Studio Autopilot ready. External providers: '+CFG.externalProviders.length);
</script></body></html>`;
}

export function handleStudioAutopilot(request){
  const url=new URL(request.url);
  if(url.pathname==='/api/studio/autopilot/capabilities'&&request.method==='GET')return json(manifest());
  if(url.pathname==='/api/studio/autopilot/health'&&request.method==='GET')return json({ok:true,service:'atlas-studio-autopilot',externalProviders:[]});
  if(url.pathname==='/studio/autopilot'&&request.method==='GET')return new Response(page(),{status:200,headers:HTML_HEADERS});
  return null;
}
