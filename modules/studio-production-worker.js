const J={
  'content-type':'application/json; charset=utf-8',
  'cache-control':'no-store',
  'access-control-allow-origin':'*'
};
const H={
  'content-type':'text/html; charset=utf-8',
  'cache-control':'no-store',
  'x-content-type-options':'nosniff',
  'referrer-policy':'same-origin'
};

const CAPABILITIES=[
  {id:'ingest',name:'Media Ingest',class:'local',state:'ready',detail:'Local file import, metadata inspection, preview and project manifest.'},
  {id:'smartcut',name:'Smart Cut',class:'local',state:'ready',detail:'Motion-based local analysis proposes a strong visual window without uploading media.'},
  {id:'timeline',name:'Timeline & Trim',class:'local',state:'ready',detail:'Real start/end trimming and frame-accurate preview controls.'},
  {id:'look',name:'Look Engine',class:'local',state:'ready',detail:'Brightness, contrast, saturation and layout treatments rendered into export.'},
  {id:'vertical',name:'Vertical Reframe',class:'local',state:'ready',detail:'9:16 crop or fit-with-blurred-background rendering for Reels and Shorts.'},
  {id:'captions',name:'Captions',class:'local',state:'ready',detail:'Import SRT, add manual cues, preview and burn captions into exports.'},
  {id:'voice-cleanup',name:'Voice Cleanup',class:'local',state:'ready',detail:'Browser audio high-pass filtering and dynamics compression during render.'},
  {id:'export',name:'Render & Export',class:'local',state:'ready',detail:'Canvas + MediaRecorder export where supported by the browser.'},
  {id:'transcribe',name:'AI Transcription',class:'model',env:'ATLAS_TRANSCRIBE_ENDPOINT',detail:'Optional authenticated transcription runtime.'},
  {id:'avatar',name:'Digital Twin',class:'model',env:'ATLAS_AVATAR_ENDPOINT',detail:'Optional consent-gated identity/avatar runtime.'},
  {id:'lipsync',name:'Lip Sync',class:'model',env:'ATLAS_LIPSYNC_ENDPOINT',detail:'Optional lip-sync runtime for replacement or generated audio.'},
  {id:'enhance',name:'AI Enhance / Upscale',class:'model',env:'ATLAS_ENHANCE_ENDPOINT',detail:'Optional super-resolution and restoration runtime.'},
  {id:'video-edit',name:'Generative Video Restyle',class:'model',env:'ATLAS_VIDEO_EDIT_ENDPOINT',detail:'Optional identity-preserving video-to-video runtime.'}
];

function runtimeConfig(env={}){
  const model=CAPABILITIES.filter(c=>c.class==='model').map(c=>({
    id:c.id,
    name:c.name,
    configured:Boolean(env[c.env]),
    requiredEnv:c.env,
    detail:c.detail
  }));
  return {
    mode:'local-first',
    localProcessing:true,
    mediaLeavesDeviceByDefault:false,
    authGatewayReady:Boolean(env.ATLAS_STUDIO_RUNTIME_TOKEN),
    model
  };
}

function json(body,status=200){return new Response(JSON.stringify(body,null,2),{status,headers:J});}

function shell(config){
  const cfg=JSON.stringify(config).replace(/</g,'\\u003c');
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>ATLAS Studio Production Engine</title>
<style>
:root{--bg:#020611;--panel:#071321;--panel2:#0a1b2c;--line:#1c4160;--text:#f5f8fc;--muted:#8fa6bb;--cyan:#5edcff;--blue:#2187ff;--green:#46d99a;--amber:#ffc95f;--red:#ff6f79;--purple:#a987ff}
*{box-sizing:border-box}html{background:var(--bg)}body{margin:0;color:var(--text);background:radial-gradient(circle at 72% -12%,#123b69 0,transparent 34%),var(--bg);font:14px Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;min-height:100vh}.app{display:grid;grid-template-columns:244px minmax(0,1fr);min-height:100vh}.side{position:sticky;top:0;height:100vh;padding:17px 12px;border-right:1px solid #16334d;background:rgba(2,7,15,.96);overflow:auto}.brand{font-weight:900;letter-spacing:.24em;font-size:22px;padding:7px 9px}.sub{color:var(--cyan);font-size:9px;letter-spacing:.15em;padding:0 9px 18px}.nav{display:flex;align-items:center;gap:9px;padding:10px;border-radius:9px;color:#9fb2c4;text-decoration:none;border:1px solid transparent;margin:2px 0}.nav:hover,.nav.on{background:#0a2844;border-color:#214d70;color:#fff}.n{width:22px;color:#65dafa}.privacy{margin:18px 8px 8px;padding:10px;border:1px solid #27533f;background:#09251c;border-radius:10px;color:#9be4c3;font-size:10px;line-height:1.45}.main{min-width:0}.top{position:sticky;top:0;z-index:10;height:66px;display:flex;align-items:center;gap:10px;padding:0 20px;border-bottom:1px solid #17364f;background:rgba(2,7,15,.86);backdrop-filter:blur(18px)}.top b{margin-right:auto}.chip{display:inline-flex;align-items:center;gap:6px;border:1px solid #285577;background:#0a2237;border-radius:999px;padding:6px 9px;font-size:9px;color:#cbe9ff}.dot{width:7px;height:7px;border-radius:50%;background:var(--green)}.content{padding:20px;max-width:1580px;margin:auto}.hero{display:flex;gap:16px;padding:22px;border:1px solid var(--line);border-radius:17px;background:linear-gradient(135deg,#0b2847,#07121e)}.hero h1{font-size:32px;margin:5px 0 8px}.hero p{margin:0;max-width:850px;color:#a9bdce;line-height:1.55}.eyebrow{font-size:9px;letter-spacing:.14em;color:#76e4ff}.hero .actions{margin-left:auto;display:flex;gap:8px;align-items:start;flex-wrap:wrap}.btn{border:1px solid #2c658d;background:#0c3152;color:#fff;border-radius:9px;padding:9px 12px;cursor:pointer}.btn:hover{border-color:#4ca8df}.btn.primary{background:linear-gradient(135deg,#1478dc,#1ba9d7);border-color:#43c7f3}.btn.warn{border-color:#8a6430;background:#3b2a0c;color:#ffe1a1}.btn:disabled{opacity:.45;cursor:not-allowed}.grid{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(320px,.65fr);gap:12px;margin-top:12px}.card{border:1px solid var(--line);background:linear-gradient(145deg,#091827,#06101b);border-radius:14px;padding:14px;min-width:0}.card h2{font-size:14px;margin:0 0 11px}.stage{position:relative;min-height:560px;border:1px solid #204c6d;background:#02050a;border-radius:13px;overflow:hidden;display:grid;place-items:center}.stage video{display:none}.stage canvas{max-width:100%;max-height:72vh;width:auto;height:auto;background:#000}.empty{color:#708ba2;text-align:center;padding:35px}.empty b{display:block;color:#c8d9e8;margin-bottom:7px}.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.field{width:100%;background:#050d17;border:1px solid #214964;color:#fff;border-radius:8px;padding:9px;margin:4px 0}.label{display:block;font-size:9px;color:#88a4bb;text-transform:uppercase;letter-spacing:.08em;margin-top:9px}.twocol{display:grid;grid-template-columns:1fr 1fr;gap:8px}.threecol{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.rangeLine{display:grid;grid-template-columns:90px 1fr 52px;gap:8px;align-items:center;margin:9px 0}.rangeLine span{font-size:10px;color:#9bb1c5}.rangeLine output{text-align:right;font-size:10px;color:#d9ecff}.timeline{height:76px;border:1px solid #1e4665;border-radius:10px;margin-top:10px;position:relative;background:linear-gradient(180deg,#071827,#040b13)}.track{position:absolute;left:10px;right:10px;top:24px;height:28px;border-radius:7px;background:repeating-linear-gradient(90deg,#0c3556 0 42px,#0a2944 42px 84px);overflow:hidden}.trimBand{position:absolute;top:0;bottom:0;border:1px solid #5bc9ff;background:rgba(32,151,224,.2);pointer-events:none}.playhead{position:absolute;top:-8px;bottom:-8px;width:2px;background:#fff;box-shadow:0 0 8px #fff}.meta{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:10px}.metric{padding:10px;border:1px solid #163d5d;border-radius:10px;background:#071421}.metric span{display:block;color:#7895ad;font-size:9px}.metric b{display:block;margin-top:5px;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.panel{display:none}.panel.on{display:block}.status{padding:10px;border-radius:9px;border:1px solid #2a526f;background:#081c2c;color:#9eb7cb;font-size:10px;line-height:1.5}.status.good{border-color:#26634d;background:#08281e;color:#a2e8c7}.status.bad{border-color:#6b3e42;background:#281014;color:#ffb2b8}.status.warn{border-color:#6a582d;background:#2c230d;color:#ffdda0}.cue{display:grid;grid-template-columns:78px 78px 1fr auto;gap:7px;align-items:center;padding:7px 0;border-bottom:1px solid #14324a;font-size:10px}.cue button{padding:4px 7px}.capList{max-height:220px;overflow:auto}.runtimeGrid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.runtime{padding:11px;border:1px solid #1c435f;border-radius:11px;background:#071625}.runtime h3{margin:0 0 6px;font-size:11px}.runtime p{margin:0;color:#829db4;font-size:9px;line-height:1.4}.runtime .tag{display:inline-block;margin-top:8px;padding:4px 6px;border-radius:999px;font-size:8px;border:1px solid #5e512b;color:#ffd88b}.runtime.ready .tag{border-color:#2e6e55;color:#9ae9c5}.log{height:145px;overflow:auto;background:#030912;border:1px solid #173650;border-radius:9px;padding:9px;font:10px ui-monospace,SFMono-Regular,Menlo,monospace;color:#9dc4df}.log div{margin:0 0 5px}.progress{height:8px;border-radius:99px;background:#07111d;border:1px solid #193b55;overflow:hidden;margin-top:10px}.progress>div{height:100%;width:0;background:linear-gradient(90deg,#237cf4,#5ce2ff);transition:width .15s}.download{display:none;margin-top:10px;padding:11px;border:1px solid #2d654e;background:#09281e;border-radius:10px}.download.on{display:block}.download a{color:#9ce9c8}.footerNote{margin:15px 0 40px;color:#6f899f;font-size:10px;line-height:1.5}.kbd{border:1px solid #2d526f;background:#071725;border-radius:5px;padding:1px 5px;font-size:9px}.compareLine{position:absolute;top:0;bottom:0;width:2px;background:#fff;pointer-events:none;box-shadow:0 0 8px #000}.hidden{display:none!important}@media(max-width:1050px){.app{grid-template-columns:1fr}.side{display:none}.grid{grid-template-columns:1fr}.stage{min-height:430px}.runtimeGrid{grid-template-columns:1fr 1fr}}@media(max-width:640px){.content{padding:11px}.top{padding:0 12px}.top .chip:nth-of-type(2){display:none}.hero{display:block}.hero .actions{margin-top:13px}.meta{grid-template-columns:1fr 1fr}.twocol,.threecol,.runtimeGrid{grid-template-columns:1fr}.stage{min-height:360px}.cue{grid-template-columns:60px 60px 1fr}.cue button{display:none}.rangeLine{grid-template-columns:72px 1fr 46px}}
</style>
</head>
<body>
<div class="app">
<aside class="side">
  <div class="brand">ATLAS</div><div class="sub">STUDIO · PRODUCTION ENGINE</div>
  <a class="nav" href="/studio"><span class="n">←</span>Studio Command Center</a>
  <a class="nav on" href="#workbench"><span class="n">01</span>Media Workbench</a>
  <a class="nav" href="#smartcut"><span class="n">02</span>Smart Cut</a>
  <a class="nav" href="#look"><span class="n">03</span>Look Engine</a>
  <a class="nav" href="#captions"><span class="n">04</span>Captions</a>
  <a class="nav" href="#models"><span class="n">05</span>Model Runtimes</a>
  <a class="nav" href="#export"><span class="n">06</span>Export</a>
  <div class="privacy">LOCAL-FIRST<br>Video stays on this device for browser-native editing unless a model runtime is explicitly configured and invoked.</div>
</aside>
<main class="main">
<header class="top"><b>ATLAS Studio Production Engine</b><span class="chip"><i class="dot"></i>Local engine ready</span><span class="chip" id="modelChip">Model runtimes: checking</span></header>
<div class="content">
<section class="hero"><div><span class="eyebrow">CLEAN-ROOM MEDIA PRODUCTION · NO FAKE CONNECTED STATES</span><h1>One production surface from ingest to export.</h1><p>ATLAS combines local media editing, motion-based cut analysis, vertical reframing, caption burn-in, audio cleanup and browser rendering. Model-backed features remain visibly gated until an authorized runtime is configured.</p></div><div class="actions"><label class="btn primary" for="fileInput">Choose video</label><input id="fileInput" class="hidden" type="file" accept="video/*"><button class="btn" id="manifestBtn">Project manifest</button></div></section>

<div class="grid" id="workbench">
  <div class="card">
    <h2>Preview & timeline</h2>
    <div class="stage" id="stage"><video id="video" playsinline preload="metadata"></video><canvas id="canvas" width="540" height="960"></canvas><div class="empty" id="empty"><b>No media loaded</b>Select a local video. The browser will decode it without uploading it to ATLAS.</div><div class="compareLine hidden" id="compareLine"></div></div>
    <div class="toolbar"><button class="btn" id="playBtn" disabled>Play</button><button class="btn" id="setInBtn" disabled>Set in</button><button class="btn" id="setOutBtn" disabled>Set out</button><button class="btn" id="compareBtn" disabled>Before / After</button></div>
    <div class="timeline"><div class="track" id="track"><div class="trimBand" id="trimBand"></div><div class="playhead" id="playhead"></div></div></div>
    <div class="meta"><div class="metric"><span>Duration</span><b id="mDuration">—</b></div><div class="metric"><span>Source</span><b id="mSize">—</b></div><div class="metric"><span>Trim</span><b id="mTrim">—</b></div><div class="metric"><span>File</span><b id="mFile">—</b></div></div>
  </div>

  <div>
    <section class="card panel on" id="smartcut"><h2>Smart Cut</h2><p class="status">Local analysis samples frames and scores visual motion. It does not claim to understand speech or audience engagement.</p><label class="label">Target seconds</label><input id="cutLen" class="field" type="number" min="3" max="60" value="15"><button class="btn primary" id="analyzeBtn" disabled>Analyze strongest visual window</button><div id="cutStatus" class="status" style="margin-top:9px">Load a video to analyze.</div></section>
    <section class="card panel on" id="look" style="margin-top:12px"><h2>Look & reframe</h2><label class="label">Format</label><select class="field" id="aspect"><option value="9:16">9:16 Reel / Short</option><option value="16:9">16:9 Landscape</option><option value="1:1">1:1 Square</option></select><label class="label">Reframe mode</label><select class="field" id="fitMode"><option value="blur">Fit + blurred background</option><option value="crop">Center crop</option><option value="contain">Contain on black</option></select><div class="rangeLine"><span>Brightness</span><input id="brightness" type="range" min="70" max="140" value="100"><output id="brightnessO">100%</output></div><div class="rangeLine"><span>Contrast</span><input id="contrast" type="range" min="70" max="150" value="106"><output id="contrastO">106%</output></div><div class="rangeLine"><span>Saturation</span><input id="saturation" type="range" min="50" max="160" value="106"><output id="saturationO">106%</output></div></section>
  </div>
</div>

<div class="grid" id="captions">
  <section class="card"><h2>Captions</h2><div class="twocol"><label class="btn" for="srtInput">Import SRT</label><input id="srtInput" class="hidden" type="file" accept=".srt,text/plain"><button class="btn" id="addCueBtn" disabled>Add cue at playhead</button></div><label class="label">Manual caption</label><input class="field" id="cueText" placeholder="Caption text"><div class="twocol"><input class="field" id="cueDuration" type="number" min="0.5" max="20" step="0.5" value="3"><label style="align-self:center;color:#9eb4c8;font-size:10px"><input type="checkbox" id="burnCaptions" checked> Burn captions into export</label></div><div class="capList" id="capList"><div class="status">No captions loaded.</div></div></section>
  <section class="card"><h2>Audio processing</h2><label style="display:block;margin:8px 0"><input type="checkbox" id="cleanupAudio" checked> Voice cleanup during export</label><p class="status">Browser-native cleanup applies an 80 Hz high-pass filter and dynamics compression. It is useful for speech cleanup, but it is not a neural denoiser.</p><label class="label">Audio monitoring</label><select class="field" id="monitorAudio"><option value="off">Silent while rendering</option><option value="on">Monitor through speakers</option></select></section>
</div>

<section class="card" id="models" style="margin-top:12px"><h2>Model-backed production runtimes</h2><p class="status warn">These are architecture slots for capabilities comparable to avatar generation, transcription, lip sync, super-resolution and video-to-video restyling. ATLAS will not display “connected” unless the corresponding authorized endpoint exists.</p><div class="runtimeGrid" id="runtimeGrid" style="margin-top:10px"></div></section>

<div class="grid" id="export">
  <section class="card"><h2>Render & export</h2><div class="threecol"><div><label class="label">Resolution</label><select class="field" id="resolution"><option value="720">720p class</option><option value="1080">1080p class</option></select></div><div><label class="label">FPS</label><select class="field" id="fps"><option>30</option><option>24</option></select></div><div><label class="label">Compare</label><select class="field" id="exportCompare"><option value="off">Processed only</option><option value="on">Original / processed split</option></select></div></div><button class="btn primary" id="renderBtn" disabled>Render selected range</button><button class="btn" id="cancelBtn" disabled>Cancel</button><div class="progress"><div id="progressBar"></div></div><div class="download" id="downloadBox"><b>Render complete.</b><br><a id="downloadLink" href="about:blank">Download video</a></div></section>
  <section class="card"><h2>Production log</h2><div class="log" id="log"></div><p class="footerNote">Export format depends on browser MediaRecorder support. ATLAS probes MP4/H.264 first where available, then WebM VP9/VP8. The generated file is real output from the selected trim, layout, filters and caption settings.</p></section>
</div>
</div>
</main>
</div>
<script>
const CONFIG=${cfg};
const el=id=>document.getElementById(id);
const video=el('video'),canvas=el('canvas'),ctx=canvas.getContext('2d',{alpha:false});
const analysisCanvas=document.createElement('canvas');analysisCanvas.width=120;analysisCanvas.height=68;const actx=analysisCanvas.getContext('2d',{willReadFrequently:true});
let file=null,fileUrl=null,trimIn=0,trimOut=0,cues=[],compare=false,rendering=false,cancelRender=false,lastBlobUrl=null,audioGraph=null;
function log(msg){const d=document.createElement('div');d.textContent=new Date().toLocaleTimeString()+'  '+msg;el('log').prepend(d)}
function fmt(s){if(!Number.isFinite(s))return '—';const m=Math.floor(s/60),ss=Math.floor(s%60);return m+':'+String(ss).padStart(2,'0')}
function ratioValue(){const [a,b]=el('aspect').value.split(':').map(Number);return a/b}
function outputDims(base=720){const r=ratioValue();if(r<1)return {w:base,h:Math.round(base/r)};return {w:Math.round(base*r),h:base}}
function clamp(v,a,b){return Math.max(a,Math.min(b,v))}
function filterString(processed=true,blur=0){if(!processed)return blur?'blur('+blur+'px)':'none';return 'brightness('+el('brightness').value+'%) contrast('+el('contrast').value+'%) saturate('+el('saturation').value+'%)'+(blur?' blur('+blur+'px)':'')}
function drawFitted(c,w,h,mode,processed){const sw=video.videoWidth||1,sh=video.videoHeight||1;const src=sw/sh,dst=w/h;c.save();c.filter=filterString(processed,0);if(mode==='crop'){let sx=0,sy=0,sww=sw,shh=sh;if(src>dst){sww=sh*dst;sx=(sw-sww)/2}else{shh=sw/dst;sy=(sh-shh)/2}c.drawImage(video,sx,sy,sww,shh,0,0,w,h)}else if(mode==='blur'){c.save();c.filter=filterString(processed,22);let bw,bh,bx,by;if(src>dst){bh=h;bw=h*src;bx=(w-bw)/2;by=0}else{bw=w;bh=w/src;bx=0;by=(h-bh)/2}c.drawImage(video,bx,by,bw,bh);c.restore();c.fillStyle='rgba(0,0,0,.22)';c.fillRect(0,0,w,h);let fw,fh,fx,fy;if(src>dst){fw=w;fh=w/src;fx=0;fy=(h-fh)/2}else{fh=h;fw=h*src;fy=0;fx=(w-fw)/2}c.filter=filterString(processed,0);c.drawImage(video,fx,fy,fw,fh)}else{c.fillStyle='#000';c.fillRect(0,0,w,h);let fw,fh,fx,fy;if(src>dst){fw=w;fh=w/src;fx=0;fy=(h-fh)/2}else{fh=h;fw=h*src;fy=0;fx=(w-fw)/2}c.drawImage(video,fx,fy,fw,fh)}c.restore()}
function activeCaption(t){return cues.find(x=>t>=x.start&&t<=x.end)}
function drawCaption(c,w,h,t){if(!el('burnCaptions').checked)return;const cue=activeCaption(t);if(!cue)return;c.save();const size=Math.round(Math.max(22,w*.044));c.font='700 '+size+'px system-ui,-apple-system,sans-serif';c.textAlign='center';c.textBaseline='middle';const max=w*.84,words=cue.text.split(/\\s+/),lines=[];let line='';for(const word of words){const test=(line+' '+word).trim();if(c.measureText(test).width>max&&line){lines.push(line);line=word}else line=test}if(line)lines.push(line);const lh=size*1.22,boxH=lines.length*lh+size*.7,y=h*.82-boxH/2;c.fillStyle='rgba(0,0,0,.72)';c.beginPath();c.roundRect(w*.07,y,w*.86,boxH,size*.35);c.fill();c.fillStyle='#fff';lines.forEach((ln,i)=>c.fillText(ln,w/2,y+size*.35+lh*(i+.5)));c.restore()}
function renderFrame(target=canvas,t=video.currentTime,forceCompare=null){if(!file||!video.videoWidth)return;const c=target.getContext('2d',{alpha:false});const w=target.width,h=target.height;c.clearRect(0,0,w,h);const mode=el('fitMode').value;const doCompare=forceCompare===null?compare:forceCompare;if(!doCompare){drawFitted(c,w,h,mode,true)}else{drawFitted(c,w,h,mode,false);c.save();c.beginPath();c.rect(w/2,0,w/2,h);c.clip();drawFitted(c,w,h,mode,true);c.restore();c.fillStyle='#fff';c.fillRect(w/2-1,0,2,h)}drawCaption(c,w,h,t);if(target===canvas){el('compareLine').classList.toggle('hidden',!doCompare);if(doCompare)el('compareLine').style.left='50%'}}
function resizePreview(){const d=outputDims(540);canvas.width=d.w;canvas.height=d.h;renderFrame()}
function updateMeta(){el('mDuration').textContent=fmt(video.duration);el('mSize').textContent=video.videoWidth&&video.videoHeight?video.videoWidth+'×'+video.videoHeight:'—';el('mTrim').textContent=fmt(trimIn)+' – '+fmt(trimOut);el('mFile').textContent=file?file.name:'—';const dur=video.duration||1;el('trimBand').style.left=(trimIn/dur*100)+'%';el('trimBand').style.width=((trimOut-trimIn)/dur*100)+'%';el('playhead').style.left=(clamp(video.currentTime||0,0,dur)/dur*100)+'%'}
function enableLoaded(on){['playBtn','setInBtn','setOutBtn','compareBtn','analyzeBtn','addCueBtn','renderBtn'].forEach(id=>el(id).disabled=!on)}
function setAspectFromDimensions(){resizePreview()}
['aspect','fitMode','brightness','contrast','saturation'].forEach(id=>el(id).addEventListener('input',()=>{if(id==='brightness'||id==='contrast'||id==='saturation')el(id+'O').value=el(id).value+'%';setAspectFromDimensions()}));
el('fileInput').onchange=e=>{const f=e.target.files&&e.target.files[0];if(!f)return;if(!f.type.startsWith('video/')){log('Rejected non-video input: '+f.type);return}file=f;if(fileUrl)URL.revokeObjectURL(fileUrl);fileUrl=URL.createObjectURL(file);video.src=fileUrl;video.load();el('empty').classList.add('hidden');log('Loaded local video '+file.name+' ('+Math.round(file.size/1024)+' KB)')};
video.onloadedmetadata=()=>{trimIn=0;trimOut=video.duration||0;enableLoaded(true);resizePreview();updateMeta();el('cutStatus').className='status good';el('cutStatus').textContent='Ready for local frame analysis.';log('Metadata '+video.videoWidth+'×'+video.videoHeight+', '+fmt(video.duration))};
video.ontimeupdate=()=>{updateMeta();if(!rendering)renderFrame()};video.onseeked=()=>{if(!rendering)renderFrame();updateMeta()};video.onplay=()=>{el('playBtn').textContent='Pause';loopPreview()};video.onpause=()=>el('playBtn').textContent='Play';
function loopPreview(){if(video.paused||video.ended||rendering)return;renderFrame();updateMeta();if('requestVideoFrameCallback'in video)video.requestVideoFrameCallback(()=>loopPreview());else requestAnimationFrame(loopPreview)}
el('playBtn').onclick=()=>video.paused?video.play():video.pause();el('setInBtn').onclick=()=>{trimIn=clamp(video.currentTime,0,Math.max(0,trimOut-.1));updateMeta();log('Trim in set '+fmt(trimIn))};el('setOutBtn').onclick=()=>{trimOut=clamp(video.currentTime,trimIn+.1,video.duration);updateMeta();log('Trim out set '+fmt(trimOut))};el('compareBtn').onclick=()=>{compare=!compare;el('compareBtn').textContent=compare?'Processed only':'Before / After';renderFrame()};
el('track').onclick=e=>{if(!file)return;const r=e.currentTarget.getBoundingClientRect();video.currentTime=clamp((e.clientX-r.left)/r.width*video.duration,0,video.duration)};
function waitSeek(t){return new Promise(resolve=>{let settled=false;const done=()=>{if(settled)return;settled=true;video.removeEventListener('seeked',done);resolve()};video.addEventListener('seeked',done,{once:true});video.currentTime=clamp(t,0,Math.max(0,video.duration-.02));setTimeout(done,1400)})}
function frameVector(){actx.filter='none';actx.drawImage(video,0,0,analysisCanvas.width,analysisCanvas.height);const d=actx.getImageData(0,0,analysisCanvas.width,analysisCanvas.height).data,out=new Uint8Array((analysisCanvas.width*analysisCanvas.height)/16);let j=0;for(let y=0;y<analysisCanvas.height;y+=4){for(let x=0;x<analysisCanvas.width;x+=4){const i=(y*analysisCanvas.width+x)*4;out[j++]=Math.round(d[i]*.299+d[i+1]*.587+d[i+2]*.114)}}return out}
function diff(a,b){let s=0;for(let i=0;i<a.length;i++)s+=Math.abs(a[i]-b[i]);return s/a.length}
el('analyzeBtn').onclick=async()=>{if(!file)return;video.pause();el('analyzeBtn').disabled=true;const target=clamp(Number(el('cutLen').value)||15,3,Math.min(60,video.duration));const step=Math.max(.75,Math.min(1.5,video.duration/45));const samples=[];let prev=null;el('cutStatus').className='status warn';el('cutStatus').textContent='Sampling frames locally…';log('Smart Cut analysis started');for(let t=0;t<video.duration;t+=step){await waitSeek(t);const vec=frameVector(),motion=prev?diff(vec,prev):0;samples.push({t,motion});prev=vec;el('cutStatus').textContent='Sampling '+Math.round(t/video.duration*100)+'%';await new Promise(r=>setTimeout(r,0))}const span=Math.max(1,Math.round(target/step));let best=-1,bestI=0;for(let i=0;i<=samples.length-span;i++){let score=0;for(let j=i;j<i+span;j++)score+=samples[j].motion;if(score>best){best=score;bestI=i}}trimIn=samples[bestI]?.t||0;trimOut=Math.min(video.duration,trimIn+target);video.currentTime=trimIn;updateMeta();el('cutStatus').className='status good';el('cutStatus').textContent='Suggested '+fmt(trimIn)+' – '+fmt(trimOut)+' based on visual motion. Adjust manually if needed.';el('analyzeBtn').disabled=false;log('Smart Cut suggested '+fmt(trimIn)+'–'+fmt(trimOut)+' score '+best.toFixed(1))};
function parseTime(v){const m=v.trim().replace('.',',').match(/(\\d+):(\\d+):(\\d+),(\\d+)/);if(!m)return NaN;return Number(m[1])*3600+Number(m[2])*60+Number(m[3])+Number(('0.'+m[4]))}
function parseSrt(text){const blocks=text.replace(/\\r/g,'').trim().split(/\\n\\s*\\n/);const out=[];for(const b of blocks){const lines=b.split('\\n');const ti=lines.findIndex(x=>x.includes('-->'));if(ti<0)continue;const [a,z]=lines[ti].split('-->').map(parseTime),caption=lines.slice(ti+1).join(' ').trim();if(Number.isFinite(a)&&Number.isFinite(z)&&caption)out.push({id:'c'+Date.now()+Math.random(),start:a,end:z,text:caption})}return out}
function renderCues(){if(!cues.length){el('capList').innerHTML='<div class="status">No captions loaded.</div>';return}el('capList').innerHTML=cues.sort((a,b)=>a.start-b.start).map(c=>'<div class="cue"><span>'+fmt(c.start)+'</span><span>'+fmt(c.end)+'</span><span>'+escapeHtml(c.text)+'</span><button class="btn" data-del="'+c.id+'">×</button></div>').join('');document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{cues=cues.filter(c=>c.id!==b.dataset.del);renderCues();renderFrame()})}
function escapeHtml(s){return String(s).replace(/[&<>\"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[ch]))}
el('srtInput').onchange=async e=>{const f=e.target.files&&e.target.files[0];if(!f)return;const imported=parseSrt(await f.text());cues=[...cues,...imported];renderCues();renderFrame();log('Imported '+imported.length+' SRT cues')};el('addCueBtn').onclick=()=>{const text=el('cueText').value.trim();if(!text)return;const d=clamp(Number(el('cueDuration').value)||3,.5,20),start=video.currentTime||trimIn;cues.push({id:'c'+Date.now(),start,end:Math.min(video.duration,start+d),text});el('cueText').value='';renderCues();renderFrame();log('Manual caption added at '+fmt(start))};
function buildAudioGraph(){if(audioGraph)return audioGraph;const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return null;try{const ac=new AC(),source=ac.createMediaElementSource(video),hp=ac.createBiquadFilter(),comp=ac.createDynamicsCompressor(),dest=ac.createMediaStreamDestination();hp.type='highpass';hp.frequency.value=80;comp.threshold.value=-24;comp.knee.value=28;comp.ratio.value=4;comp.attack.value=.004;comp.release.value=.22;source.connect(hp);hp.connect(comp);comp.connect(dest);audioGraph={ac,source,hp,comp,dest,monitor:false};return audioGraph}catch(err){log('Audio graph unavailable: '+err.message);return null}}
function supportedMime(){const list=['video/mp4;codecs=h264,aac','video/mp4','video/webm;codecs=vp9,opus','video/webm;codecs=vp8,opus','video/webm'];for(const m of list){try{if(window.MediaRecorder&&MediaRecorder.isTypeSupported(m))return m}catch(_){}}return ''}
async function seekAndWait(t){video.pause();await waitSeek(t)}
function nextFrame(cb){if('requestVideoFrameCallback'in video)video.requestVideoFrameCallback(cb);else requestAnimationFrame(()=>cb(performance.now(),{mediaTime:video.currentTime}))}
el('renderBtn').onclick=async()=>{if(!file||rendering)return;if(!canvas.captureStream||!window.MediaRecorder){el('downloadBox').classList.remove('on');log('This browser lacks canvas.captureStream or MediaRecorder');alert('This browser cannot render video with the local ATLAS engine. Try a current desktop browser or configure the server render runtime.');return}rendering=true;cancelRender=false;el('renderBtn').disabled=true;el('cancelBtn').disabled=false;el('downloadBox').classList.remove('on');const base=Number(el('resolution').value)||720,d=outputDims(base),renderCanvas=document.createElement('canvas');renderCanvas.width=d.w;renderCanvas.height=d.h;const fps=Number(el('fps').value)||30,stream=renderCanvas.captureStream(fps);let graph=null;if(el('cleanupAudio').checked){graph=buildAudioGraph();if(graph){await graph.ac.resume();if(el('monitorAudio').value==='on'&&!graph.monitor){graph.comp.connect(graph.ac.destination);graph.monitor=true}for(const tr of graph.dest.stream.getAudioTracks())stream.addTrack(tr)}}const mime=supportedMime(),chunks=[];let recorder;try{recorder=new MediaRecorder(stream,mime?{mimeType:mime,videoBitsPerSecond:base>=1080?8500000:4500000}:undefined)}catch(err){rendering=false;el('renderBtn').disabled=false;el('cancelBtn').disabled=true;log('MediaRecorder init failed: '+err.message);return}recorder.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data)};recorder.onerror=e=>log('Recorder error '+(e.error?.message||e.message||'unknown'));recorder.onstop=()=>{const type=recorder.mimeType||mime||'video/webm',blob=new Blob(chunks,{type});if(lastBlobUrl)URL.revokeObjectURL(lastBlobUrl);lastBlobUrl=URL.createObjectURL(blob);const ext=type.includes('mp4')?'mp4':'webm';el('downloadLink').href=lastBlobUrl;el('downloadLink').download='atlas-studio-render-'+Date.now()+'.'+ext;el('downloadBox').classList.add('on');el('progressBar').style.width='100%';rendering=false;el('renderBtn').disabled=false;el('cancelBtn').disabled=true;video.pause();log('Render complete '+Math.round(blob.size/1024)+' KB · '+type)};await seekAndWait(trimIn);recorder.start(250);log('Render started '+d.w+'×'+d.h+' @ '+fps+' fps · '+(mime||'browser default'));const duration=Math.max(.01,trimOut-trimIn),split=el('exportCompare').value==='on';video.currentTime=trimIn;await video.play();const drawLoop=()=>{if(cancelRender||video.currentTime>=trimOut||video.ended){video.pause();if(recorder.state!=='inactive')recorder.stop();return}renderFrame(renderCanvas,video.currentTime,split);el('progressBar').style.width=(clamp((video.currentTime-trimIn)/duration,0,1)*100)+'%';nextFrame(drawLoop)};nextFrame(drawLoop)};
el('cancelBtn').onclick=()=>{if(rendering){cancelRender=true;log('Render cancellation requested')}};
el('manifestBtn').onclick=()=>{const manifest={version:1,product:'ATLAS Studio Production Engine',createdAt:new Date().toISOString(),source:file?{name:file.name,type:file.type,size:file.size,duration:video.duration,width:video.videoWidth,height:video.videoHeight}:null,edit:{trimIn,trimOut,aspect:el('aspect').value,fitMode:el('fitMode').value,brightness:Number(el('brightness').value),contrast:Number(el('contrast').value),saturation:Number(el('saturation').value),captions:cues,burnCaptions:el('burnCaptions').checked,voiceCleanup:el('cleanupAudio').checked},privacy:{localFirst:true,uploadedByDefault:false}};const blob=new Blob([JSON.stringify(manifest,null,2)],{type:'application/json'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='atlas-studio-project.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);log('Project manifest exported')};
function renderRuntimes(){const models=CONFIG.model||[];const ready=models.filter(x=>x.configured).length;el('modelChip').textContent='Model runtimes: '+ready+'/'+models.length+' configured';el('runtimeGrid').innerHTML=models.map(x=>'<article class="runtime '+(x.configured?'ready':'')+'"><h3>'+escapeHtml(x.name)+'</h3><p>'+escapeHtml(x.detail)+'</p><span class="tag">'+(x.configured?'CONFIGURED':'REQUIRES '+escapeHtml(x.requiredEnv))+'</span></article>').join('')}
renderRuntimes();resizePreview();renderCues();log('ATLAS local production engine initialized');
</script>
</body></html>`;
}

export function handleStudioProduction(request,env={}){
  const u=new URL(request.url);const p=u.pathname.replace(/\/+$/,'')||'/';
  if(request.method==='OPTIONS')return new Response(null,{status:204,headers:J});
  if(p==='/api/studio/production/health'){
    const config=runtimeConfig(env);
    return json({ok:true,service:'atlas-studio-production',mode:config.mode,localProcessing:true,modelRuntimesConfigured:config.model.filter(x=>x.configured).length,modelRuntimesTotal:config.model.length,time:new Date().toISOString()});
  }
  if(p==='/api/studio/production/capabilities'){
    const config=runtimeConfig(env);
    return json({service:'atlas-studio-production',cleanRoom:true,privacy:{localFirst:true,mediaLeavesDeviceByDefault:false},capabilities:CAPABILITIES.map(c=>c.class==='model'?{...c,configured:Boolean(env[c.env])}:c),runtime:config});
  }
  if(p==='/api/studio/production/runtime-status')return json(runtimeConfig(env));
  if((p==='/studio/production'||p.startsWith('/studio/production/'))&&request.method==='GET')return new Response(shell(runtimeConfig(env)),{headers:H});
  return null;
}
